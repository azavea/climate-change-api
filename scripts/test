#!/bin/bash

set -eu

function usage() {
    echo -n \
"Usage: $(basename "$0")

Run application tests.

"
}

if [ "${BASH_SOURCE[0]}" = "${0}" ]
then
    if [ "${1:-}" = "--help" ]
    then
        usage
    else
        ./scripts/console django './manage.py test'
    fi
    exit
fi
